{
  "name": "webgpu-react-app",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "@types/cesium": "^1.67.14",
    "@types/jest": "^27.5.2",
    "@types/node": "^16.18.126",
    "@types/react": "^19.1.11",
    "@types/react-dom": "^19.1.8",
    "@webgpu/types": "^0.1.64",
    "@xenova/transformers": "latest",
    "ajv": "^8.18.0",
    "cesium": "^1.140.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-scripts": "^5.0.1",
    "suncalc": "^1.9.0",
    "three": "^0.160.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "prebuild": "node -e \"\nconst key = process.env.REACT_APP_MAPS_API_KEY || '';\nconst isBad = !key || /placeholder|your_|replace|example/i.test(key);\nif (isBad) {\n  console.warn('\\n⚠️  WARNING: REACT_APP_MAPS_API_KEY is missing or looks like a placeholder.');\n  console.warn('   The production bundle will contain a non-working key.\\n' +\n  '   For live deploys, either:\\n' +\n  '     1. export REACT_APP_MAPS_API_KEY=... before npm run build, OR\\n' +\n  '     2. Run with MAPS_API_KEY=... python deploy.py (it can override at deploy time).\\n');\n} else {\n  console.log('✅ Maps API key present at build time (length ' + key.length + '). Remember to also set referrer restrictions for all target hosts (test.1ink.us, go.1ink.us, localhost).');\n}\n\"",
    "build": "react-scripts build && npm run build:wasm && ./scripts/verify-build.sh",
    "build:wasm": "bash scripts/build-wasm.sh",
    "build:wasm:watch": "bash scripts/build-wasm.sh --wat-only && echo 'Run npm run build:wasm to rebuild after changes.'",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "validate:maps-key": "node -e \"\nconst k=process.env.REACT_APP_MAPS_API_KEY||''; const bad=!k||/placeholder|your_|replace/i.test(k);\nconsole.log(bad ? '❌ Key missing or placeholder' : '✅ Key looks configured ('+k.length+' chars)');\nprocess.exit(bad?1:0);\n\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/google.maps": "^3.58.1",
    "@types/suncalc": "^1.9.2",
    "@types/three": "^0.160.0",
    "playwright": "^1.60.0",
    "typescript": "^4.9.5",
    "wabt": "^1.0.39"
  }
}
