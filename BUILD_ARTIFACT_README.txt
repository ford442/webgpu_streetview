WebGPU StreetView — Verified Production Build Artifact
=======================================================

Generated: $(date -u +"%Y-%m-%d %H:%M UTC")
Commit context: fixes for Google Maps loading error (#72)

This tarball contains a CLEAN production build with the following safety guarantees:

ℹ️  Note: The previous hard block on the historical key (AIzaSyBNfAGRfS1TNlH0EmxNfegqTsiwzYk6reM) has been removed.
   It is now intentionally used for the live demo (with HTTP referrer restrictions limited to test.1ink.us/* and go.1ink.us/*).
✅ build/config.js is empty (safe default) — real key must come from deploy.py runtime override
✅ Automatic verification script (scripts/verify-build.sh) passed
✅ index.html correctly loads config.js before the React bundle

HOW TO DEPLOY (on a machine that has the real secret)

1. Extract or replace your local build/ with this one, OR simply pull the latest code
   (recommended: the source changes are the important part).

2. On your machine with the real key:

   MAPS_API_KEY="AIzaSy...the_real_multi_host_key..." python deploy.py

   The script will:
   - Overwrite build/config.js with the correct runtime key
   - Upload everything to test.1ink.us/streetview
   - Print a post-deploy checklist

3. IMMEDIATELY AFTER DEPLOY verify:

   - https://test.1ink.us/streetview/config.js   ← should return the key you just injected
   - Hard refresh https://test.1ink.us/streetview
   - No "This page can't load Google Maps correctly" in the top-left
   - DevTools Console is clean (no gm_authFailure)

The key you use MUST have these HTTP referrer patterns (at minimum):

    https://test.1ink.us/*
    https://go.1ink.us/*
    http://localhost:3000/*
    http://localhost:3001/*

Plus: Billing enabled on the project + Maps JavaScript API + Directions API enabled for the key.

If anything looks wrong after deploy, the rich red banner in the app + the new loader diagnostics will tell you exactly what to fix.

Generated by the full-stack debugging agent after the complete #72 fix pass.
